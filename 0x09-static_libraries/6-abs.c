#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 *@n: integer
 *
 * Return: absolute value of an integer (Success)
 */
int _abs(int n)
{
int i;
i = abs(n);
return (i);
}
