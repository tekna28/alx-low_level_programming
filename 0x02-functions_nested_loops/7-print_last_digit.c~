#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - prints the last digit of a number.
 *
 *@n: integer
 * Return: the last digit of a number (Success)
 */
int print_last_digit(int n)
{
int digit;
digit = n % 10;
return (digit);
}
